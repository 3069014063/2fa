name = "2fa"
main = "src/worker.js"
compatibility_date = "2024-01-13"
workers_dev = true

# KV 命名空间
# 始终声明 SECRETS_KV，避免 Git 自动构建部署时丢失绑定。
# 新版 Wrangler 会在首次部署时自动创建或保持已链接的 KV 资源。
# 如需固定到指定资源，可在本地补充 id / preview_id。
[[kv_namespaces]]
binding = "SECRETS_KV"
id = "bb7f3030b32a441a937d841e501846c2"
preview_id = "b6c4090a117c46ce94cf0bd2a88ddedb"
[triggers]
crons = ["0 16 * * *"]

# ====================================
# 🚀 Service Worker 自动版本管理
# ====================================
# SW_VERSION 将在部署时通过 scripts/generate-version.js 自动生成
# 支持三种版本策略：
#   - 时间戳（默认）：v20250102-123456
#   - Git commit：v<short-hash>
#   - Package版本：v1.0.0
#
# 使用方式：
#   npm run deploy              # 使用时间戳版本
#   npm run deploy:git          # 使用 git commit 版本
#   npm run deploy:package      # 使用 package.json 版本
#
# 注意：版本号由部署脚本注入，此处的值仅用于本地开发
[vars]
SW_VERSION = "v1"
ENVIRONMENT = "production"
[env.development]
name = "2fa-dev"

# 绑定项不可继承，开发环境也需要显式声明。
[[env.development.kv_namespaces]]
binding = "SECRETS_KV"
id = "27891dd17d854dad8de14448763fed4d"

[env.development.vars]
ENVIRONMENT = "development"
SW_VERSION = "v1"
